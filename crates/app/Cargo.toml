[package]
name = "noisegate"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[[bin]]
name = "noisegate"
path = "src/main.rs"

[dependencies]
audio-io = { package = "noisegate-audio-io", path = "../audio-io" }
dsp = { package = "noisegate-dsp", path = "../dsp" }

anyhow.workspace = true
thiserror.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
serde.workspace = true
toml.workspace = true
ringbuf.workspace = true
crossbeam-channel.workspace = true

dirs = "5"

[target.'cfg(windows)'.dependencies]
tray-icon = "0.19"
muda = "0.15"
# Only what we actually call: the single-instance mutex and the console-mode
# toggle for the banner. Registry/keyboard/process-enumeration bindings were
# unused — no reason to keep the surface around.
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_System_Threading",
    "Win32_System_Console",
] }
winit = { version = "0.30", default-features = false, features = ["rwh_06"] }

# Future: embed a real .ico via the `embed-resource` crate. v1 generates
# its tray icon procedurally so no build.rs is needed.
